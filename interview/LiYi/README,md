1. SQL(view + storeprocedure + index + rightjoin / crossjoin / outerjoin) -> SQL SERVER
   No relationship DB(Splunk) -> basic idea get to know
   
2. java datastructure 
   graph (best route) -> closet route
    -> singelton pattern / mvc pattern / command line pattern... usual patterns
   in which case use which pattern	
   	
   string reverse / duplicate... process
   Vector / Queue / Stack
   
avfuel -> linkedin
